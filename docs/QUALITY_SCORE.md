# 质量与验证证据

状态：验收中 · 负责人：项目维护者 · 核验日期：2026-09-07

此表记录真实证据，不能把“已编写检查”当作“已通过检查”。启动阶段完成后更新最终结果。

| 领域 | 当前证据 | 尚待验证 |
| --- | --- | --- |
| 计算/状态/数据模型 | `pnpm test`：49 个测试通过，含 37 组语言契约；三位小数/十位整数边界、续算精度、x/混合括号、旧配置迁移（含六套旧主题向纸白/午夜迁移）通过 | 最终 UI 联动回归 |
| 原生持久化 | `pnpm check:native` 通过；2 个综合测试覆盖读写/备份、损坏/失败保护、旧目录迁移、目标冲突、最终配置提交失败与重开路径 | 系统文件夹选择和最终原生构建 |
| 类型与构建 | 本次 `pnpm typecheck` 通过 | 最新统一检查与发行构建 |
| 文档与架构 | 已同步设置、存储路径、六主题、显示精度、未完成表、架构与回归流程 | 最新文档/架构门禁 |
| 浏览器 UI | 明暗切换实测（2026-09-07，端口 1430 隔离预览）：侧栏设置行最右侧太阳/月亮图标即时切换纸白/午夜、图标与可访问名称随模式变化、刷新后主题保留；设置弹窗已无主题配色块；375/1280 宽度无页面横向溢出；切换与缩放期间控制台无错误。弹窗点击外部关闭、数字键焦点、最大负数完整显示为此前实测 | 768/1440 布局、最终统一检查与控制台；旧六套主题的浏览器证据已随收敛过期，仅保留迁移测试 |
| 桌面 UI | 2026-09-07：0.2.x 调试版以隔离目录启动通过；Overlay 标题栏布局经浏览器 `native-titlebar` 模拟验证（品牌行 x=80 避让交通灯、顶栏顶到窗口上沿、收起侧栏后顶栏左内边距 84px）；顶部拖拽处理器对区域内空白/面包屑/字标触发、对按钮输入框排除的选择器逻辑经浏览器实测；`desktop:build --bundles app` 产出 Calcbook.app（Info.plist 版本与源码一致）并实机启动/退出正常 | 拖拽移动窗口的实际手感需实机手动确认；原生目录切换、导出、关闭重开仍未验证 |
| 性能 | 旧基线：300 行/20 次，中位 8.92 ms、p95 12.67 ms | 本次语法扩展后的基准 |
| CI | 配置随代码纳入仓库 | 未在远端执行 |

验证记录不得包含真实用户笔记正文。测试使用仓库示例或独立的 `Harness 验收` 笔记。已知限制归入 [债务表](exec-plans/tech-debt.md)。

当前仍在验收，尚未完成项不得写作通过。收尾时用实际命令结果、截图路径和原生文件检查替换上表的待验证项，再归档执行计划。
